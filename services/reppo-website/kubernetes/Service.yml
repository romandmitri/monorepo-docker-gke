apiVersion: v1
kind: Service
metadata:
  name: reppo-website
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: reppo-website
  # https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: NodePort
